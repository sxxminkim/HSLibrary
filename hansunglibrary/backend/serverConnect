const mysql=require('aws-sdk')
const rds=new AWS.RDS({
    endpoint:'hslibrary.cxnldqrql9ej.ap-southeast-2.rds.amazonaws.com',
    port:3306,
    database:'hslibrary',
    user:'admin',
    password:'hslibrary'
});
rds.connect((err)=>{
    if(err){
        console.err('AWS RDS 연결 실패 : ',err.stack);
        return;
    }
    console.log('AWS RDS 연결 성공');
})